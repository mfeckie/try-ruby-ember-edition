<h1>Let's Learn Ruby!</h1>

<div class="row">
    <div class="large-12 columns">
        <ul class="small-block-grid-3">
            {{#each model as |lesson|}}
                <li>
                    <div class="lessons action" {{action 'goToLesson' lesson}}>
                        <div class="panel">
                            <h4>Lesson {{lesson.lessonNumber}}</h4>

                            <p>{{lesson.lessonTitle}}</p>
                        </div>
                    </div>
                </li>
            {{/each}}
        </ul>
    </div>
</div>
